-- The theme marketplace moved to a GitHub-PR catalog bundled at build time
-- (Orang-Studio/orangchat-marketplace), alongside plugins and profile themes.
-- Community themes are no longer uploaded to or stored by the server, so the
-- table and its host-only review flow are gone.

DROP TABLE IF EXISTS "Theme";
